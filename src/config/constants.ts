export const DEFAULT_PROPS_MESSAGE = "Hello from ";
export const API_URL = import.meta.env.VITE_API_URL;
// This is for testing purposes only
export const getAPIUrl = () => {
  const url = import.meta.env.VITE_API_URL;
  return url;
};
